export const config = {
    width: 8,
    height: 8,
    cellSize: 64,

    seed: 0, // integer

    octaves: 1, // integer
    lacunarity: 2, // number, + = more frequency, - = less frequency
    persistance: 0.5, // 0-1, + = more amplitude, - = less amplitude
}
